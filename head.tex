%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode

\documentclass[14pt, a4paper, onecolumn]{report}

% XeLaTex檢查點, 以要求必須使用XeLaTex來處理模版
\usepackage{ifxetex}
\ifxetex\else\errmessage{模版: 請使用XeLaTex來產生論文.}\stop\fi

% 設定邊界
\usepackage{geometry} 
\geometry{
    a4paper,
    top = 3.2cm, bottom = 2.5cm, 
    left = 3.8cm, right = 2.5cm,
}

% 設定封面的邊界設定
\newcommand{\EnableCoverPageStyle}{
    \newgeometry{
        top = 4cm, bottom = 3cm,
    }
}
\newcommand{\DisableCoverPageStyle}{\restoregeometry}

% 引用xeCJK
\usepackage{xeCJK}
% 使用標楷體作為中文字體
\setCJKmainfont{DFKaiShu-SB-Estd-BF}
% 使用 Times New Romes 作為英文字體
\setmainfont{Times-Roman}

% 預設前幾頁的頁碼是用羅馬數字
\pagenumbering{roman}
